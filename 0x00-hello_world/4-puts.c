#include <stdio.h>

/**
 * main - Entry point
 * puts function to print a string
 * Return: 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
